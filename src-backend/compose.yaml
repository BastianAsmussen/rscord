services:
  server:
    build:
      context: .
      target: final
    ports:
      - 8080:8080
    environment:
      - DATABASE_URL=postgres://postgres:password@db:5432/rscord
    depends_on:
      db:
        condition: service_healthy
    develop:
      watch:
        - action: rebuild
          path: src/
        - action: rebuild
          path: Cargo.toml
        - action: rebuild
          path: Cargo.lock
        - action: rebuild
          path: migrations/

  db:
    image: postgres:18.1-trixie
    restart: always
    user: postgres
    secrets:
      - db-password
    volumes:
      - db-data:/var/lib/postgresql
    environment:
      - POSTGRES_DB=rscord
      - POSTGRES_PASSWORD_FILE=/run/secrets/db-password
    ports:
      - 5432:5432
    healthcheck:
      test: ["CMD", "pg_isready"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  db-data:

secrets:
  db-password:
    file: db/password.txt
