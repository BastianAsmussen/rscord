import { A } from "@solidjs/router";

export default function Settings() {
    return (
        <div>
            <h1>Settings Page</h1>

            <nav>
                <A href="/">Back to Home</A>
            </nav>
        </div>
    );
}
